{
  "name": "cogent",
  "version": "1.0.0",
  "description": "Cogent - AI-powered documentation platform",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npx convex dev\"",
    "build": "npm run build --prefix frontend",
    "convex:dev": "npx convex dev",
    "convex:deploy": "npx convex deploy"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "convex": "^1.25.4"
  }
}